package calculator.core.uniquedigit;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UniqueDigitCalculationRepository
    extends JpaRepository<UniqueDigitCalculation, Long> {}
